import { create } from 'zustand';
import { Inventory } from './InventoryTypes';

export interface InventoryState {

};

export const useInventoryStore = create<InventoryState>()((set, get, store) => ({
  
}));
